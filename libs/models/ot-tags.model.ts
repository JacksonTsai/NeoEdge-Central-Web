export interface IOtTagsForUI {
  tagName: string;
  enable: boolean;
  tagType: { value: string };
  function: { value: number };
  startAddress: number;
  quantity: number;
  trigger: { value: string };
  interval: number;
  Quantity: number;
}
