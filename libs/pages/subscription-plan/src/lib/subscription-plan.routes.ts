import { Route } from '@angular/router';
import { SubscriptionPlanPageComponent } from './containers';

export const subscriptionPlanRoutes: Route[] = [{ path: '', component: SubscriptionPlanPageComponent }];
